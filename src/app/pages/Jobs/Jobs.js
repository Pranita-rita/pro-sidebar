import React, { Component } from 'react'
import Layout from '../../components/SideBar/Layout'

export default class Jobs extends Component {
  render() {
    return (
     <Layout title='Jobs' {...this.props}>

     </Layout>
    )
  }
}
